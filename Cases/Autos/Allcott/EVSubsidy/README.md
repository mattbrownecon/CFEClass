# EVSubsidy

### Data sources

Vehicle data was copied from the EVSubsidy folder.

### Replication

Run the `sample_solution.Rmd` file.

_Note: Each problem set solution can also be replicated with `bash make.sh` from the root of that problem set’s folder._
